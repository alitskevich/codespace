import { addNextIds } from "./addNextIds";
import { toColor, toColorText } from "./color";
import { idiomsHash } from "./idiomsHash";
import { parseText } from "./parser";

// resource bundle: strings, metadata, pipes functions
export const functions = {
  toColor, toColorText, parseText, idiomsHash, addNextIds
};