<component id="Chip">
  <span data="{data}" click="{click}" aria-selected="{isSelected}" class="
    min-w-10 px-2 py-2
    inline-flex gap-1 items-center
    rounded-lg
    font-semibold text-sm
    transition duration-300
    
    bg-gray-400 dark:bg-gray-500
    text-gray-100 dark:text-gray-200

    hover:bg-slate-300 dark:hover:bg-slate-700

    aria-selected:bg-purple-600 aria-selected:text-white

    {click ? 'cursor-pointer'} {class}
    ">
    <Icon type="{icon}" If="icon" />
    <span class="mx-1" If="title">{title}</span>
    <Slot />
    <span If="badge" class="inline-block py-0 px-1 ml-1 
      leading-none text-center whitespace-nowrap align-baseline
      text-sm rounded bg-slate-600 text-slate-100">
      {badge}
    </span>
  </span>
</component>