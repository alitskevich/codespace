<component id="Icon">
  <span aria-selected="{selected}" class="
        flex items-center justify-center rounded-md
        h-{size ?? 6 | plus: 2} w-{size ?? 6 | plus: 2}
        {click ? R.styles.klikable} 
        {class}
        " title="{title ?? type}" data="{data}" click="{click}">

    <Dynamic As="{ns ?? 'Svg'}.{type | capitalize}" class="h-{size ?? 6} w-{size ?? 6} {iconClass}" />

    <span class="sr-only">{title ?? type}</span>

  </span>
</component>