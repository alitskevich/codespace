// import { readFileJsonContent } from "ultimus-fs";

// import { generateColors } from "../generators/generateColors";

/**
 * Normalize the raw figma data and write the normalized nodes and meta files to the specified directory.
 *
 * @param {Object} contractDir - The directory where the files will be written.
 * @return {void} This function does not return anything.
 */
export function doGenerateColors() {
  // const src = readFileJsonContent([contractDir, `ui.colors.json`]);

  // const result = generateColors(src);

  // writeFileContent([contractDir, `darkmode.css`], result);

  console.log(`✅ Dark mode colors has been mapped.`);
}
